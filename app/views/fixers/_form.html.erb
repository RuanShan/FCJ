  <% i=0%>
    <% if @object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@object.errors.count, "error") %> prohibited this <%=@object_class.model_name.human%> from being saved:</h2>

      <ul>
      <% @object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <% @object_class.new_cols.size.fdiv(3).ceil.times{ |i| %>
      <p>
  <%   for col in @object_class.new_cols.slice(i*3,3)%>
      <%=label_tag "object[#{col}]", @object_class.human_attribute_name(col) %>
      <%= text_field_tag "object[#{col}]",@object[col]  %>   
    <% end %>
      </p>
  <% }%>
